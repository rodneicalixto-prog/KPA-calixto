# Decisions

Registre decisoes que afetam o rumo do projeto.

| Data | Decisao | Premissa | Impacto | Reversivel |
|---|---|---|---|---|
| 2026-08-27 | Criadas 10 Skills (`02_AGENTS/skills/kpa-*`) empacotando os agentes core do V30 (CoS, Orchestrator, Researcher, Strategist, Copy Director, Production Lead, Traffic Analyst, Product Auditor, QA Editor, Memory Curator) com triggers ativaveis | Nenhum desses agentes tinha SKILL.md; convertidos seguindo `21_BUILDER_KIT/scaffolds/skill-scaffold.md`, cada um validado contra sua task (`03_TASKS/`), diretriz (`04_DIRETRIZES/`), gate (`00_OS/gates.md`) e handoff (`00_OS/handoffs.md`) | Agentes core agora ativaveis por frase-gatilho como as outras stacks (Trafego/WhatsApp/Automacao) | Sim — sao arquivos novos, remover nao afeta os agentes originais em `02_AGENTS/*.md` |
| 2026-08-27 | Nao foi criada skill `kpa-automation-architect` | Pre-flight (regra do Forge) encontrou skill equivalente ja existente: `18_AUTOMATION_STACK/skills/process-automation-design/SKILL.md` cobre a mesma task (T09), mesmo gate (GATE-AUTOMATION) e mesmos outputs (blueprint/SOP/matriz/teste/rollback) | Evita skill duplicada | Sim — pode ser criada depois se o conteudo divergir |
| 2026-08-27 | Adicionada definicao de `GATE-MEMORY` em `00_OS/gates.md` | `02_AGENTS/kpa-memory-curator.md` ja citava `GATE-MEMORY` mas o gate nunca tinha sido definido em `00_OS/gates.md` nem `00_OS/gate-matrix.md` — usados os criterios ja descritos no proprio agente | Fecha lacuna de gate ausente; QA Editor agora tem criterio pra validar output do Memory Curator | Sim — e so documentacao, pode ser editado |
| 2026-08-27 | Registrada divergencia de `owner` em `03_TASKS/T08-product-hardening-lp-audit.md` (diz `CoS`, mas quem executa e o Product Auditor) | Nao corrigido automaticamente por ser mudanca em contrato de task existente; documentado como nota dentro de `02_AGENTS/skills/kpa-product-auditor/SKILL.md` | Nenhum imediato; fica registrado pra correcao humana decidir se e erro de digitacao | Sim — trocar o campo `owner` no task file quando confirmado |
| 2026-08-27 | Seguido `21_BUILDER_KIT/checklists/index-update-checklist.md` a partir do `kpa-orchestrator` pra fechar a implantacao das 10 skills: criado `08_CHECKLISTS/gate-memory.md`, atualizado `00_OS/cos.md` (tabela de classificacao agora aponta pras SKILL.md novas em vez do `.md` de agente puro, mais linhas novas pra "funil completo" e "organizar memoria") e `00_INDEX.md` (rotas principais) | O checklist de "GATE NOVO" exigia o checklist do gate; a tabela de classificacao do CoS ainda apontava pro agente bruto (`02_AGENTS/*.md`) em vez da skill completa com triggers/workflow — sem isso a skill nova nunca seria de fato acionada pelo CoS | Pedidos de estrategia/mecanismo/memoria/funil completo agora resolvem pro arquivo certo (smoke test via grep confirmado) | Sim — sao edicoes de tabela e um checklist novo |
| 2026-08-27 | Nao foram criadas SKILL.md genericas pros 8 agentes do Traffic Stack; em vez disso, criadas as 4 tasks que faltavam (`analisar-criativos.md`, `investigar-queda.md`, `escalar-vencedores.md`, `espionar-concorrente.md`) | Investigacao mostrou que os 8 agentes de `11_TRAFFIC_STACK/agents/` ja sao personas completas e auto-suficientes (ativacao, comandos `*`, routing table proprios) — SKILL.md generico seria redundante e pior. O gap real: a `routing_table` de `@traffic-orchestrator` apontava pra 4 arquivos de task inexistentes, ou seja `*criativos`, `*queda`, `*escalar` e `*espiar` quebravam na pratica | 8/8 comandos do `@traffic-orchestrator` agora tem task real por tras; `python3 11_TRAFFIC_STACK/tests/validate_stack.py` confirma 49 artefatos validos | Sim — arquivos novos, documentacao apenas |
| 2026-08-27 | Varredura completa do repo por referencias `` `arquivo.md` `` quebradas (script proprio, comparando contra o filesystem real). Corrigido: 2 templates faltantes em `12_WHATSAPP_STACK/templates/` (`variables-and-tags.md`, `test-cases.md`, referenciados por `cowork-automation-architect.md` mas nunca criados); criados `.claude/commands/<nome>.md` pros 8 commands de `00_OS/commands/` + `meta-cli-install` (exigido por `21_BUILDER_KIT/conventions.md`, nenhum existia — nenhum comando `/` do kit funcionava como slash-command nativo do Claude Code); corrigidos varios caminhos obsoletos `15_PRODUCT_RELEASE/exemplos\|nichos\|cowork\|prompts/...` que deveriam ser `exemplos/\|nichos/\|cowork/\|prompts/...` (pastas foram movidas pra raiz do repo em algum momento e a documentacao nao acompanhou) em `22_CLAUDE_DESKTOP/knowledge-files.md`, `00_OS/commands/instalar-kpa30.md` e `20_MCP_SETUP/connectors/gmail.md` | Cada correcao foi validada contra o filesystem real antes de aplicar (nunca so "parece certo") | MCPs/comandos/templates que antes apontavam pro vazio agora resolvem pro arquivo certo | Sim — sao edicoes de path e arquivos novos |
| 2026-08-27 | Nao criados: `docs/setup-whatsapp.md` (referenciado por `docs/guia-implementacao-rapida.md` e `docs/guia-referencia-rapida.md`), nem `.claude/agents/briefing-agent.md`/`criacao-agent.md` (referenciados em `22_CLAUDE_DESKTOP/knowledge-files.md`, ja marcados la como "ainda nao existe") | `docs/guia-implementacao-rapida.md` descreve uma estrutura de pastas legada e incompativel com o V30 atual (`produto/agentes/`, 5 agentes fixos, `.claude/agents/` com README) — corrigir so o link quebrado sem revisar o doc inteiro deixaria o guia ainda errado. `docs/README.md` confirma que essa pasta e "origem: PDFs publicos e docs do kit-base legado" que precisa ficar consistente com V30, ou seja e uma reescrita de conteudo, nao um bug mecanico | Nenhum ainda — fica pendente pra decisao humana (reescrever docs/ pro V30 atual, ou aposentar esses guias legados) | Sim — nada foi apagado, so nao criado |

| 2026-08-27 | "Implantacao total do kit": 3 agentes Explore mapearam tudo que faltava; fechado em 4 frentes (A/B/C/D) | Usuario pediu explicitamente pra fechar todo gap restante, inclusive autorizando reescrita de conteudo legado e criacao de comandos novos apos eu levantar o escopo real | Ver linhas abaixo por frente | Sim — tudo documentacao/arquivo novo, nada destrutivo |
| 2026-08-27 | Fase A: criados 28 wrappers `.claude/agents/<nome>.md` (11 de `02_AGENTS/`, 8 de `11_TRAFFIC_STACK/agents/`, 7 de `12_WHATSAPP_STACK/agents/`, 1 de `18_AUTOMATION_STACK/agents/`, mais `forge`) | `21_BUILDER_KIT/conventions.md` exige wrapper pra qualquer agente funcionar nativamente no Claude Code; nenhum existia antes desta rodada (so `.claude/commands/` tinha sido fechado na rodada anterior) | Todo agente do kit agora e invocavel nativamente, nao so por leitura manual do CoS | Sim — wrappers triviais (front-matter + "leia e siga") |
| 2026-08-27 | Fase A: esclarecida referencia a `14_NICHE_KITS/` em `18_AUTOMATION_STACK/agents/automation-orchestrator.md` (marcada como produto futuro opcional, com `04_DIRETRIZES/nichos/` como fonte primaria); adicionado comentario em `00_OS/clients-map.yaml` esclarecendo que `act-mapping.yaml` por cliente e a fonte de verdade do `act_id`, nao o mapa global; `22_CLAUDE_DESKTOP/knowledge-files.md` trocou `briefing-agent.md`/`criacao-agent.md` (nunca existiram) pelos comandos reais `/briefing` e `/criar` | Gaps de baixo risco (tinham fallback) mas que geravam confusao sobre qual arquivo e a fonte de verdade | Nenhuma mudanca de comportamento, so clareza documental | Sim |
| 2026-08-27 | Fase B: criados 11 comandos novos em `00_OS/commands/` com conteudo real (`briefing`, `criar`, `revisar`, `entregar`, `relatorio`, `proposta`, `onboarding`, `follow-up`, `diagnostico`, `automatizar-processo`, `primeira-tarefa`) + `setup-nicho.md` fino (aponta pra `nichos/setup-nicho-playbook.md` que ja tinha conteudo) + wrapper `.claude/commands/mcp-setup.md` fino (aponta pra `20_MCP_SETUP/commands/mcp-setup.md`) — todos com wrapper `.claude/commands/` | `22_CLAUDE_DESKTOP/commands-keywords.md` anunciava 18 comandos mas so 8 existiam de verdade; usuario confirmou criar todos apos eu levantar o escopo (conteudo novo real, nao so correcao de link). Cada comando reusa especialista/task/gate ja existente, nenhum mecanismo novo foi inventado | Todo comando citado em `commands-keywords.md` agora existe de verdade (18 comandos + `forge` coberto como agente) | Sim — arquivos novos, seguindo o mesmo formato Objetivo/Passos/Saida dos 8 originais |
| 2026-08-27 | Fase C: reescritos `docs/guia-implementacao-rapida.md` e `docs/guia-referencia-rapida.md` (decisao humana pendente da rodada anterior — usuario optou implicitamente por "reescrever" ao pedir "implantacao total"); criado `docs/setup-whatsapp.md` (nunca existiu, referenciado 2x); ajustes pontuais em `docs/glossario.md` (entrada Briefing, termos novos CoS/Forge/Gate/Skill), `docs/troubleshooting.md` (item 6, sobre `.claude/commands/` e `.claude/agents/` que agora existem de verdade), `docs/faq.md` (respostas 8 e 11, Claude Desktop e Forge) | Os 2 guias descreviam estrutura legada (`produto/agentes/`, `/briefing-agent`) incompativel com o V30 atual e com link morto confirmado; agora refletem o fluxo real (`instalar kpa30`, comandos da Fase B, `.claude/agents/` da Fase A) | Novo usuario que seguir os guias de `docs/` agora cai em comandos/pastas que realmente existem | Sim — reescrita de conteudo, nada de estrutura de codigo |
| 2026-08-27 | Corrigida sintaxe invalida `claude mcp add <nome> --command "..."` (11 arquivos) pra `claude mcp add <nome> -- ...`; corrigido `rube` de stdio (`npx -y @composio/rube-mcp`, que na pratica e so um CLI helper, nao um servidor MCP stdio) pra `--transport http rube https://rube.app/mcp` em 3 arquivos + JSON de exemplo em 2 arquivos (`instalar-kpa30.md`, `composio-rube.md`, `claude-desktop-config.json`) | Confirmado ao vivo nesta sessao: `claude mcp add --help` nao tem flag `--command`, e `npx -y @composio/rube-mcp` sem subcomando so imprime `--help`, nao roda como servidor. Bug real que ja tinha me pego durante a instalacao inicial (rodada 1) | Qualquer mentorado que copiar e colar esses comandos agora funciona de primeira, sem precisar descobrir a sintaxe certa sozinho | Sim — comandos de exemplo em markdown |

> Quando uma decisao for tomada com impacto no produto/projeto, anote aqui. Inclua premissa (por que decidiu assim), impacto (o que muda) e reversibilidade (consegue desfazer? como?).
